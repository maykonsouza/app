<div class="conteiner">
  <div class="row">

    <p id="notice"><%= notice %></p>

    <%= image_tag @host.avatar.url(:thumb), class:'figure-img img-fluid rounded' %>
    <%= image_tag @host.avatar.thumb.url %>
    <%#= image_tag @host.avatar.url(:thumb) %>

    <p>
      <strong>Name:</strong>
      <%= @host.name %>
    </p>

    <p>
      <strong>Address:</strong>
      <%= @host.address %>
    </p>

    <%= link_to 'Edit', edit_host_path(@host) %> |
    <%= link_to 'Back', hosts_path %>

    <figure class="figure">
      <%= image_tag @host.avatar.thumb.url %>
    </figure>


  <div>
<div>


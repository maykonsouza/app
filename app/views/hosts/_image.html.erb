<%# <style type="text/css">
table , td, th {
	border: 1px solid #595959;
  border-collapse: collapse;
  
}
td, th {
	padding: 3px;
	width: 250px;
	height: 25px;
}
th {
	background: #f0e6cc;
}
.even {
	background: #fbf8f0;
}
.odd {
	background: #fefcf9;
}

</style> %>

<div class="row"> 
    
  <% @hosts.each do |host| %>
    <div class="shadow p-3 mb-5 bg-white rounded mr-3 figure-img img-fluid">
      <table>
        <tbody>
          <tr>
            <td rowspan="2"><%= image_tag host.avatar.thumb.url %></td>           
              <td class="align-bottom"><p class="lead"><%= host.name %></p></td>
          </tr>
            <tr>
              <td class="align-top"><h5><small class="text-muted align-top "><%= host.address %></small></h5></td>
            </tr>
          <tr>
            <td colspan="2"><%= image_tag("https://www.hdm-cpa.com/wp-content/uploads/2014/03/350x250.gif", :alt => "rss feed") %></td>
          </tr>
          <tr>
            <td class="align-middle"><p class="text-center">Latência: 10ms</p></td>
            <td class="align-middle"><p class="text-center">Qualidade: Boa </p></td>
          </tr>
        </tbody>
      </table>
    </div>
  <% end %>

</div>

